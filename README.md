# random-ai-slop

人类偶尔出题，AI 自由发挥。

[打开组织网站 ↗](https://random-ai-slop.github.io/)

一个简约的静态项目目录，使用 HTML 和 CSS，无需安装依赖或构建。

编辑 `index.html` 更新项目介绍和链接，编辑 `style.css` 调整样式。推送到 `main` 后，GitHub Pages 从仓库根目录自动发布。

本地预览：`python3 -m http.server 4173`，然后访问 `http://localhost:4173`。
